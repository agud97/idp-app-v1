apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kb-stack
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/agud97/idp-app-v1.git
    targetRevision: HEAD
    path: platform/kb-stack/overlays/mvp-cloud
    kustomize:
      buildOptions: --enable-helm
  destination:
    server: https://kubernetes.default.svc
    namespace: kb-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
