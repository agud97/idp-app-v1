apiVersion: batch/v1
kind: CronJob
metadata:
  name: kubescape-exporter
  namespace: kb-system
spec:
  schedule: "*/30 * * * *"
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: kubescape-exporter
        spec:
          serviceAccountName: kubescape-exporter
          restartPolicy: OnFailure
          containers:
            - name: scanner
              image: quay.io/kubescape/kubescape:v3.0.0
              envFrom:
                - configMapRef:
                    name: kb-stack-config
              command:
                - /bin/sh
                - -c
                - |
                  set -euo pipefail
                  out="/tmp/kubescape-findings.json"
                  kubescape scan framework nsa --format json --output "$out" || true
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
            - name: uploader
              image: minio/mc:RELEASE.2024-12-18T00-45-54Z
              envFrom:
                - configMapRef:
                    name: kb-stack-config
              env:
                - name: MINIO_ACCESS_KEY
                  valueFrom:
                    secretKeyRef:
                      name: minio-credentials
                      key: accessKey
                - name: MINIO_SECRET_KEY
                  valueFrom:
                    secretKeyRef:
                      name: minio-credentials
                      key: secretKey
              command:
                - /bin/sh
                - -c
                - |
                  set -euo pipefail
                  ts=$(date -u +"%Y%m%dT%H%M%SZ")
                  mc alias set local "$MINIO_ENDPOINT" "$MINIO_ACCESS_KEY" "$MINIO_SECRET_KEY"
                  mc mb -p "local/kb-artifacts" || true
                  mc cp "/tmp/kubescape-findings.json" "local/kb-artifacts/raw/kubescape/${CLUSTER_ID}/${ts}/findings.json"
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir: {}
